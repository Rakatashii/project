module ApplicationHelper
  # "Application helper should be avoided, as it usually just becomes a place to dump unused code later."
  def full_title(page_title = '')
    base_title = "Ruby on Rails Tutorial Sample App"
    if page_title.empty?
      base_title
    else
      page_title + " | " + base_title
    end
  end
end
